/* Permite mostrar el logo */
document.addEventListener("DOMContentLoaded", function () {
    setTimeout(() => {
        document.getElementById("intro").classList.add("visible");
    }, 500); // Retraso para suavizar la transición de la imagen
});